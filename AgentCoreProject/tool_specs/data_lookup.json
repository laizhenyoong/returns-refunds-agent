[
  {
    "name": "order_lookup",
    "description": "Look up an order by customer ID and product ID, returning its status and purchase date.",
    "inputSchema": {
      "type": "object",
      "description": "Input for looking up an order in the workshop-orders table.",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The unique identifier of the customer who placed the order, e.g. C-01."
        },
        "product_id": {
          "type": "string",
          "description": "The unique identifier of the ordered product, e.g. P-001."
        }
      },
      "required": ["customer_id", "product_id"]
    }
  },
  {
    "name": "user_lookup",
    "description": "Look up a customer's profile by customer ID, returning their name and country code.",
    "inputSchema": {
      "type": "object",
      "description": "Input for looking up a customer in the workshop-customers table.",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The unique identifier of the customer, e.g. C-01."
        }
      },
      "required": ["customer_id"]
    }
  },
  {
    "name": "product_lookup",
    "description": "Look up a product by product ID, returning its name, category, and provider.",
    "inputSchema": {
      "type": "object",
      "description": "Input for looking up a product in the workshop-products table.",
      "properties": {
        "product_id": {
          "type": "string",
          "description": "The unique identifier of the product, e.g. P-001."
        }
      },
      "required": ["product_id"]
    }
  },
  {
    "name": "find_returned_products",
    "description": "List every order currently in the RETURNED state across all customers, each enriched with its product name. Takes no input and returns a 'returned_products' array (customer_id, product_id, product_name, purchased_date, status) plus a 'count'.",
    "inputSchema": {
      "type": "object",
      "description": "No input required; this tool scans the workshop-orders table for all RETURNED orders.",
      "properties": {}
    }
  },
  {
    "name": "process_refund",
    "description": "Process a refund by setting an order's status to REFUNDED. An order is identified by customer ID plus product ID, since orders have no standalone order ID. This writes to the orders table, so confirm the order and refund amount with the administrator before calling it. Returns the updated order with its product details, or an error if no such order exists.",
    "inputSchema": {
      "type": "object",
      "description": "Identifies the order to refund in the workshop-orders table.",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The unique identifier of the customer who placed the order, e.g. C-01."
        },
        "product_id": {
          "type": "string",
          "description": "The unique identifier of the ordered product, e.g. P-001."
        }
      },
      "required": ["customer_id", "product_id"]
    }
  }
]
